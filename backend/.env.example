# Database configurations
DATABASE_URL=postgresql://tailorforge_admin:temp_password_change_me@localhost/tailorforge

# Vector Database (Qdrant) configurations
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Document Parsing Service (Docling) URL
PARSER_SERVICE_URL=http://localhost:8081

# JWT token security signing key (use a secure random string for local isolation)
SECRET_KEY=super-secret-dev-key

# LLM API keys (Optional if provided dynamically via frontend settings)
GEMINI_API_KEY=
